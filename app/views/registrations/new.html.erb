<h1>Sign Up</h1>

<%= form_with model: @user, local: true, url: sign_up_path do |form| %>
    <div class="mb-3">
        <%= form.label :email %>
        <%= form.text_field :email, class: "form-control", placeholder: "mojemail@domena.com" %>
    </div>

    <div class="mb-3">
        <%= form.label :password %>
        <%= form.password_field :password, class: "form-control", placeholder: "heslo"%>
    </div>

    <div class="mb-3">
        <%= form.label :password_confirmation %>
    <%= form.password_field :password_confirmation, class: "form-control", placeholder: "zopakuj heslo"%>
    </div>

    <div class="mb-3">
        <%= form.submit "Registrovať sa", class: "btn btn-primary" %>
    </div>

<% end %>